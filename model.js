//this is our model, we have the bill amount(price), tip(also a price number),percentage(percentage number), total = amoun*(1+percentage/100)
initModel = {
    billAmount: 0,
    tip: 0,
    percentage: 0,
    total: 0
}

module.exports = {
    initModel
}
